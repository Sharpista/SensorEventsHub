@inject HttpClient Http;
@using SensorEventsHub.UI.Client.Models

@if(eventoNordeste == null)
{
    <p><em>Loading...</em></p>
}
else
{
<div class="container-fluid">
    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">Sensor</th>
                <th scope="col">Quantidade</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>brasil.sensor.nordeste</td>
                <td>@ValorNordeste()</td>
            </tr>

        </tbody>
    </table>
</div>
    
}

@code {
    private Evento [] eventoNordeste;


    protected override async Task OnInitializedAsync()
    {
        await LoadData();
    }

    protected async Task LoadData()
    {
        eventoNordeste = await Http.GetJsonAsync<Evento []>("http://localhost:55897/api/sensores/nordeste");
    }
    private int ValorNordeste()
    {
        var count = 0;

        foreach(var item in eventoNordeste)
        {
            count += int.Parse(item.Valor);
        }

        return count;
    }
}



